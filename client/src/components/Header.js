import React, { useState } from "react";

const Header = () => {
	return (
		<div className="header-main">
			<h1 className="text-center mt-5">Polly</h1>
			<h3 className="text-center mb-4">A poll app built with React </h3>
		</div>
	);
};

export default Header;
